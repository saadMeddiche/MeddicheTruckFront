export enum InputType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  IMAGE = 'image',
  DATE = 'date',
  DATETIME = 'datetime',
  TIME = 'time',
  NUMBER = 'number',
  SELECT = 'select',
  MULTI_SELECT = 'multi-select',
  CHECKBOX = 'checkbox',
}
