export enum InputType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  IMAGE = 'image',
  DATE = 'date',
  DATETIME = 'datetime',
  NUMBER = 'number',
  SELECT = 'select',
  MULTI_SELECT = 'multi-select'
}
